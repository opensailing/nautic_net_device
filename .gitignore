# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# direnv
/.envrc

# macOS Finder
.DS_Store

# Target-specific native artifacts are written through Mix's priv symlink.
/priv/native/

/tmp/

# editor swap files
*.swp

# fwup firmware signing keys. fwup-key.priv is a long-lived SECRET (back it up in
# 1Password); the .pub is registered with the NervesHub org + baked into firmware.
fwup-key.priv
fwup-key.pub
*.fw
signed*.fw
